describe("UI initializer", function () {
    it("adds LanguageBar on allowed pages", function () {

    });

    it("doesn't add LanguageBar on not allowed pages", function () {

    });

    it("adds LanguageBar only once", function () {

    });
});